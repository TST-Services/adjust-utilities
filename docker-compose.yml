version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - ADJUST_AUTH_TOKEN=${ADJUST_AUTH_TOKEN}
    env_file:
      - .env
    restart: unless-stopped
